export type EducationEntry = {
  degree: string;
  school: string;
  dateRange: string;
  detail: string;
};

export type Achievement = {
  label: string;
  detail?: string;
};

export type CommunityRole = {
  role: string;
  org: string;
  detail?: string;
};
